            // if(ch == '.'){
            //     if(s.empty() && s2.empty()) return 0;
            //     if(s.empty() && !s2.empty()) cout <<"yes";
            //     else if(!s.empty()) cout <<"no";
            //     cout<<'\n';
            //     while( !s.empty() ) s.pop();
            //     while( !s2.empty() ) s.pop();
            //     break;
            // }